# 0038 — String Methods

Split `"a,b,c"` on commas, upper-case each part, and join them with `-`, printing `A-B-C`. Identical `split`/`map`/`join` chain to JavaScript, with the array's `string[]` type tracked through the pipeline. The methods are erased to the same runtime calls.

## Run

    npx tsx main.ts
