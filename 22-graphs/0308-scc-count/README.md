# 0308 — Strongly Connected Components

Count the strongly connected components of 0->1,1->2,2->0,2->3, printing `2`. Tarjan's algorithm tracks discovery `index` and `low`-link values over typed arrays and a stack.

## Run

    npx tsx main.ts
