# 0304 — Connected Components

Count the connected components of the undirected graph with edges (0,1),(1,2),(3,4) over nodes 0..4, printing `2`. A BFS over a typed adjacency list flood-fills each unseen node.

## Run

    npx tsx main.ts
