# 0301 — Directed Cycle Detection

Detect a cycle in the digraph 0->1,1->2,2->0, printing `cycle`. A DFS coloring (white/gray/black) modeled as a TypeScript `enum` flags the back edge.

## Run

    npx tsx main.ts
