# 0143 — Type Alias

Define a semantic alias for an integer type (e.g. Meters), assign 5, and print `distance: 5`. A `type` alias gives `number` a domain-specific name without runtime cost.

## Run

    npx tsx main.ts
