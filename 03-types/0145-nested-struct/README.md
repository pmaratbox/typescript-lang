# 0145 — Nested Struct

Model a person whose address is a nested struct, and print the city `London`. Nested `interface` types compose into plain object literals accessed by dotted paths.

## Run

    npx tsx main.ts
