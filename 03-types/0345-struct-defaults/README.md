# 0345 — Struct Defaults

Create a Point with default fields (0,0), then one overriding x to 5, printing each on its own line `0 0` then `5 0`. A factory spreads `Partial` overrides onto defaulted field values.

## Run

    npx tsx main.ts
