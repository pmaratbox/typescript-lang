# 0144 — Flag Enum (Bit Flags)

Combine bit flags READ(1) and WRITE(2) into 3, check that WRITE is set, and print `3 yes`. A numeric `enum` plus bitwise `|` and `&` models flag sets idiomatically.

## Run

    npx tsx main.ts
