# 0344 — Enum Ordinal

For the enum Direction(N,E,S,W) print the ordinal of S (2) and the name at ordinal 3 (W), printing `2 W`. A numeric `enum` assigns sequential values and supports reverse mapping from value back to name.

## Run

    npx tsx main.ts
