# 0342 — Tagged Union Eval

Model an expression tagged union Num | Add and evaluate Add(Num 1, Num 2), printing `3`. A discriminated union with a `tag` field lets `switch` narrow each variant idiomatically.

## Run

    npx tsx main.ts
