# 0146 — Numeric Conversions

Truncate 3.9 to the int 3 and widen the int 3 to the float 3.0, printing `3 3.0`. `Math.trunc` rounds toward zero and `toFixed(1)` formats the widened value with one decimal.

## Run

    npx tsx main.ts
