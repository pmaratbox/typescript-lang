# 0343 — Optional Chaining

Read a deeply optional value a.b.c when present (5) and when absent (default 0), printing `5 0`. Optional chaining `?.` short-circuits to `undefined`, and `??` supplies the fallback.

## Run

    npx tsx main.ts
