# 0396 — Call Counter

Wrap a function so each call increments a counter; after five calls print `calls: 5`. A closure-style wrapper carries a `calls` property mutated on each invocation.

## Run

    npx tsx main.ts
