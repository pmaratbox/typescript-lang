# 0131 — Assertions

Assert several expected equalities (all true) and print `all passed`. A tiny generic `assertEqual` helper throws on mismatch, so reaching the end means every fact held.

## Run

    npx tsx main.ts
