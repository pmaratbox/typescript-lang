# 0133 — Debug Representation

Produce a debug/structured representation of a Point(1,2) and print `Point(x=1, y=2)`. A custom `toString` on the `Point` class yields the exact structured form.

## Run

    npx tsx main.ts
