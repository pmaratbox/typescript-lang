# 0132 — Mini Test Runner

Run three named test functions that all pass and report `3 passed, 0 failed`. Each test is a closure in a list; the runner executes them, catching thrown errors to tally passes versus failures.

## Run

    npx tsx main.ts
