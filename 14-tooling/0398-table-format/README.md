# 0398 — Aligned Table

Format the rows (a,1),(bb,22) with the first column left-padded to the widest value, printing two aligned rows. `padEnd` with the computed max width left-aligns each first cell before joining with " | ".

## Run

    npx tsx main.ts
