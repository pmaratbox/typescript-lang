# 0399 — Progress Bar

Render a width-10 progress bar at 40% completion, printing `[####------]`. `String.prototype.repeat` builds the filled and empty segments from the rounded fill count.

## Run

    npx tsx main.ts
