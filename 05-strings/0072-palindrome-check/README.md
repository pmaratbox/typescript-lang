# 0072 — Palindrome Check

Check whether each of `level` and `hello` reads the same forwards and backwards, printing `level: yes` and `hello: no`. The same spread-reverse-join comparison as JavaScript, with `isPal` typed `boolean`.

## Run

    npx tsx main.ts
