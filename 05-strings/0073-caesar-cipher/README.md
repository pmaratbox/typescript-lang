# 0073 — Caesar Cipher

Encrypt `abc` with a Caesar cipher shifting each letter forward by `1` (wrapping within the alphabet) and print the result: `bcd`. The same code-unit arithmetic as JavaScript; `97` anchors `a` and `% 26` wraps.

## Run

    npx tsx main.ts
