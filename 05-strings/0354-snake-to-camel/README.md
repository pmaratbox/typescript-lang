# 0354 — Snake to Camel Case

Convert "hello_world" to camelCase `helloWorld`. Destructuring the split parts keeps the first word lowercase while mapping the rest in idiomatic TypeScript.

## Run

    npx tsx main.ts
