# 0156 — Count Substring Occurrences

Count non-overlapping occurrences of "ab" in "ababab", printing `3`. Scan with indexOf, advancing past each match by the needle length.

## Run

    npx tsx main.ts
