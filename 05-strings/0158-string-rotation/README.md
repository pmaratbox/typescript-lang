# 0158 — String Rotation Check

Check whether "cdab" is a rotation of "abcd", printing `yes`. b is a rotation of a iff same length and b is a substring of a+a, checked with includes.

## Run

    npx tsx main.ts
