# 0061 — Reverse a String

Reverse the string `abc` character by character and print the result: `cba`. The same code-point spread-reverse-join as JavaScript; the string type carries no extra runtime behavior.

## Run

    npx tsx main.ts
