# 0077 — Title Case

Convert `hello world` to title case (uppercase the first letter of each word) and print it: `Hello World`. The same first-letter uppercasing as JavaScript, mapped over the split words.

## Run

    npx tsx main.ts
