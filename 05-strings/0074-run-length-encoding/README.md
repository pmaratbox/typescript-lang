# 0074 — Run-Length Encoding

Run-length encode the string `aaabbc` (each run of a repeated character becomes the character followed by its count), printing `a3b2c1`. The same run-counting loop as JavaScript; `result` is a `string` built up run by run.

## Run

    npx tsx main.ts
