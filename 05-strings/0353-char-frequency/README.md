# 0353 — Character Frequency

Count character frequencies in "aab" and print them in first-seen order `a:2 b:1`. A `Map` preserves insertion (first-appearance) order in idiomatic TypeScript.

## Run

    npx tsx main.ts
