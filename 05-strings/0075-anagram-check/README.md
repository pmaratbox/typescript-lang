# 0075 — Anagram Check

Check whether each pair is an anagram by comparing sorted letters — `listen`/`silent` and `hello`/`world` — printing `listen/silent: yes` and `hello/world: no`. The same sort-and-compare as JavaScript; `pairs` is typed as a list of string tuples.

## Run

    npx tsx main.ts
