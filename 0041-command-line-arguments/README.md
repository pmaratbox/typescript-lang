# 0041 — Command-line Arguments

Read the first command-line argument and greet it, so running with `Ada` prints `hello, Ada`. The same `process.argv[2]` as Node; `tsx` runs the file directly and forwards the arguments unchanged. The `string` typing is implicit from `process.argv`.

## Run

    npx tsx main.ts Ada
