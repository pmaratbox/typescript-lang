# 0427 — Filter Operator

Use the library's filter operator to keep the even values of 1..6. Uses RxJS's `filter` operator over a `from([1..6])` source to emit only the even values.

## Run

    npx tsx main.ts
