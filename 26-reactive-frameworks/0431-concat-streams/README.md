# 0431 — Concat Streams

Use the library's concat operator so the second stream starts only after the first completes. Uses RxJS with the `concat` operator to join two synchronous `of` streams in order.

## Run

    npx tsx main.ts
