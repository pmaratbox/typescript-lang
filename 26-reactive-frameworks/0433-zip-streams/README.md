# 0433 — Zip Streams

Use the library's zip operator to pair two streams by index and combine each pair as a+b. Uses RxJS `zip` with `map` to add the paired emissions synchronously.

## Run

    npx tsx main.ts
