# 0430 — Merge Streams

Use the library's merge operator on a virtual/test scheduler to interleave two timed streams by emission time. Built with RxJS `merge` over `timer` sources driven by `TestScheduler.run`.

## Run

    npx tsx main.ts
