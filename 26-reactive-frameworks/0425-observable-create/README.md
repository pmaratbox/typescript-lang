# 0425 — Create a Stream

Create a stream of 1, 2, 3 with your language's reactive library and print each value, then print done on completion. Uses RxJS with the `of` creation operator and a `subscribe` next/complete observer.

## Run

    npx tsx main.ts
