# 0666 — Big integer multiplication

Multiply two large integers exactly using TypeScript's built-in `BigInt` type. The product `123456789 * 987654321` is computed with the `*` operator on `BigInt` literals, yielding `121932631112635269` with no floating-point rounding.

## Run

    npx tsx main.ts
