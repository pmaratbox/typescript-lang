# 0668 — Factorial

Compute 30! (factorial) exactly using TypeScript's built-in arbitrary-precision `BigInt` type. A `BigInt` literal (the `n` suffix) and the `*` operator multiply the integers 2 through 30 without any float rounding, yielding the exact 33-digit result.

## Run

    npx tsx main.ts
