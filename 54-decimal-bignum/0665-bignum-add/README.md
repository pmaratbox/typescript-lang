# 0665 — Big integer addition

Add two huge integers using TypeScript's native arbitrary-precision `BigInt` type. The `+` operator on two `bigint` literals (`12345678901234567890n` and `98765432109876543210n`) computes the exact sum with no floating-point rounding, yielding `111111111011111111100`.

## Run

    npx tsx main.ts
