# 0670 — Exact decimal multiplication

Multiply `1.1 * 1.1` exactly using the `BigNumber` type from `bignumber.js`. The `.times()` method performs base-10 decimal arithmetic, so the product is exactly `1.21` with no binary floating-point rounding error.

## Run

    npx tsx main.ts
