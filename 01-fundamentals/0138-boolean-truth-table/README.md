# 0138 — Boolean Truth Table

For each (a,b) in (T,T),(T,F),(F,T),(F,F) print a, b, and a AND b, a OR b, a XOR b as lowercase booleans, one row per line. Booleans interpolate as lowercase `true`/`false`, and XOR is expressed as `a !== b`.

## Run

    npx tsx main.ts
