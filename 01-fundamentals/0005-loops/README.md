# 0005 — Loops

Print 1..5 with a classic C-style three-part `for` loop. Same shape as
JavaScript; the `: number` annotation is optional — TypeScript would infer
it. The same `for-of` form (`for (const x of iterable)`) works for
collection iteration.

## Run

    npx tsx main.ts
