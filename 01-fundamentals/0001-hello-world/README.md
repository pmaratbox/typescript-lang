# 0001 — Hello, world!

A minimal TypeScript program: declares a `string`-typed constant and prints it.
The `: string` annotation is redundant here (TypeScript would infer it) but is
shown to introduce the type-annotation syntax.

## Run

    npx tsx main.ts
