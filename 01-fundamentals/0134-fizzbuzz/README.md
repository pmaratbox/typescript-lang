# 0134 — FizzBuzz

Print 1..15, replacing multiples of 3 with Fizz, of 5 with Buzz, and of both with FizzBuzz, one per line. A small helper function returns the string for each number, called in a `for` loop.

## Run

    npx tsx main.ts
