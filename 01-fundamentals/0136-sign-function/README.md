# 0136 — Sign Function

Print the sign (-1, 0, or 1) of -5, 0, and 5: `-1 0 1`. A typed `sign` helper returns -1/0/1 via comparisons; results are joined with spaces in a template literal.

## Run

    npx tsx main.ts
