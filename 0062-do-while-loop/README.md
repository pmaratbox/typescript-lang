# 0062 — Do-While Loop

Use a do-while loop (whose body runs once before the condition is checked) to print `1`, `2`, and `3` on one line: `1 2 3`. The same `do { ... } while (cond)` as JavaScript; the body always runs at least once.

## Run

    npx tsx main.ts
