# 0676 — Edge existence

Build the fixed weighted undirected graph G with the `graphlib` `Graph` type and use its `hasEdge` method to test edge membership. We query whether edge `b-c` exists (it does) and whether edge `a-e` exists (it does not), printing the two booleans lowercase and space-joined.

## Run

    npx tsx main.ts
