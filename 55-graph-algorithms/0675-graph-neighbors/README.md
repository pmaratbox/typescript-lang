# 0675 — Neighbors

Build the fixed weighted undirected graph G with the `graphlib` library, then query the neighbors of node `a`. `graphlib`'s `Graph.neighbors(v)` returns the adjacent nodes; the result is sorted and comma-joined for deterministic output: `b,c`.

## Run

    npx tsx main.ts
