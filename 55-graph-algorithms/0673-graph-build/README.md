# 0673 — Build a graph

Builds the fixed weighted undirected graph G with the `graphlib` library and
prints the result of its `nodeCount()` and `edgeCount()` accessors — the
library tracks both as edges are inserted via `setEdge`.

## Run

    npx tsx main.ts
