# 0674 — Node degree

Build the fixed weighted undirected graph G with the `graphlib` library and compute the degree of node `b`. Since `b` is connected to `a`, `c`, and `d`, its degree is `3`. We use `graphlib`'s `Graph.neighbors()` accessor and count the incident nodes rather than hardcoding the answer.

## Run

    npx tsx main.ts
