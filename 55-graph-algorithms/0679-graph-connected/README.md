# 0679 — Connectivity

Build the fixed weighted undirected graph G with the `graphlib` `Graph` type and use its `alg.components` algorithm to find the connected components. Nodes `a` and `e` are reachable from one another exactly when they belong to the same component, so we test for that and print the boolean lowercase.

## Run

    npx tsx main.ts
