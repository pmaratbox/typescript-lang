# 0199 — Struct Field Names

List the field names of a Point{x,y} struct/record and print `x y`. TypeScript interfaces are erased at runtime, so introspect a concrete value with `Object.keys`.

## Run

    npx tsx main.ts
