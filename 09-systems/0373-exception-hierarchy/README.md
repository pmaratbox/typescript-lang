# 0373 — Exception Hierarchy

Throw a specific error subtype and catch it through a base-type handler, printing `caught base`. TypeScript uses `instanceof` to match a subtype error against its supertype.

## Run

    npx tsx main.ts
