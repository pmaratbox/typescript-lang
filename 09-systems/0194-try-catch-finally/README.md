# 0194 — Try / Catch / Finally

Throw and catch an error, printing `caught`, and always run a finally block printing `cleanup`, on two lines. TypeScript uses the same `try`/`catch`/`finally` as JavaScript, with the caught value typed `unknown`.

## Run

    npx tsx main.ts
