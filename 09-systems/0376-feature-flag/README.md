# 0376 — Feature Flag

Branch on a boolean feature flag: on -> "enabled", off -> "disabled", printing `enabled disabled`. A ternary evaluates the same code path under both flag states.

## Run

    npx tsx main.ts
