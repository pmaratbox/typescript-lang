# 0374 — Error Codes

Map the numeric error code 2 to its message "not found" and print `not found`. A typed `Record<number, string>` provides the code-to-message lookup.

## Run

    npx tsx main.ts
