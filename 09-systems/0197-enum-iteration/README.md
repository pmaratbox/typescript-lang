# 0197 — Enum Iteration

Iterate over all values of a Color enum (RED, GREEN, BLUE) and print their names `RED GREEN BLUE`. A numeric TypeScript `enum` is reverse-mapped, so filter `Object.values` to the string keys to recover the member names.

## Run

    npx tsx main.ts
