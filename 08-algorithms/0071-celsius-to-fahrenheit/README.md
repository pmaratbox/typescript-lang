# 0071 — Celsius to Fahrenheit

Convert `100` and `0` degrees Celsius to Fahrenheit using `F = C * 9 / 5 + 32`, printing `100C = 212F` and `0C = 32F`. The same floating-point division as JavaScript; the values divide evenly, so `f` prints without decimals.

## Run

    npx tsx main.ts
