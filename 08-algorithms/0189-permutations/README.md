# 0189 — Permutations

Generate all permutations of [1,2,3] in lexicographic order, one per line space-separated. Lexicographic order exactly as shown via recursion that fixes each element in turn.

## Run

    npx tsx main.ts
