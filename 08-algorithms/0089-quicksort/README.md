# 0089 — Quicksort

Sort the list `3, 1, 4, 1, 5, 2` using quicksort (partition around a pivot, then recurse on each side) and print the result: `1 1 2 3 4 5`. The same filter-partition quicksort as JavaScript with `number[]` types.

## Run

    npx tsx main.ts
