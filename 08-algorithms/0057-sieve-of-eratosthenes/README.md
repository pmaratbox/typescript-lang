# 0057 — Sieve of Eratosthenes

Use the Sieve of Eratosthenes to find every prime number up to `10` and print them: `2 3 5 7`. The same sieve as JavaScript with a `boolean[]` flag array; the types are erased at compile time, so the emitted code matches.

## Run

    npx tsx main.ts
