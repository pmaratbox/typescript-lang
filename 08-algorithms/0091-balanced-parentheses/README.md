# 0091 — Balanced Parentheses

Using a stack, check whether `(())` is balanced and whether `(()` is balanced, printing `yes` then `no`. The same array-as-stack check as JavaScript, returning a `boolean`.

## Run

    npx tsx main.ts
