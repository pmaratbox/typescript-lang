# 0191 — Pascal Triangle

Print the first 4 rows of Pascal’s triangle, each row space-separated. Each entry is the sum of the two above; print 4 rows using `??` to default off-edge cells to 0.

## Run

    npx tsx main.ts
