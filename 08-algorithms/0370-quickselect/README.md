# 0370 — Quickselect

Find the 3rd smallest element of [7,10,4,3,20,15] with quickselect, printing `7`. Lomuto/Hoare partition recursing into one side; sorted it is [3,4,7,...], 3rd is 7, via a Lomuto-partition loop over `number[]`.

## Run

    npx tsx main.ts
