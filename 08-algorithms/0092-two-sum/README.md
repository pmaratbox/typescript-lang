# 0092 — Two Sum

Find the two indices in `2, 7, 11, 15` whose values sum to `9` and print them: `0 1`. The same single-pass `Map` lookup as JavaScript, typed `Map<number, number>`.

## Run

    npx tsx main.ts
