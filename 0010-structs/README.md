# 0010 — Structs

Define a `Person` type with a `name` and an `age`, create one ("Ada", 36), and
print each field. An `interface` (or `type`) describes the shape; the object
literal must match it or the compiler complains. The types are erased at
runtime — the emitted JavaScript is just the object.

## Run

    npx tsx main.ts
