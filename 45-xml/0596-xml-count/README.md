# 0596 — Count elements

Uses the `fast-xml-parser` library to parse a fixed catalog document, then counts the `<book>` elements by reading the length of the parsed `book` array.

## Run

    npx tsx main.ts
