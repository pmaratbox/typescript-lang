# 0595 — All attributes

Uses the `fast-xml-parser` library to parse a fixed catalog document and collect an attribute from every matching element: the `id` attribute of each `<book>`, joined with commas (`b1,b2`).

## Run

    npx tsx main.ts
