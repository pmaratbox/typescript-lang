# 0597 — Sum numeric children

Uses the **fast-xml-parser** library to parse a fixed catalog document. Each `<book>` element's `<price>` text is parsed as an integer and summed across all books (30 + 45) to print `75`.

## Run

    npx tsx main.ts
