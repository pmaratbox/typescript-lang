# 0594 — Find all elements

Uses the `fast-xml-parser` library to parse a fixed catalog document, find ALL `<book>` elements, and join each book's `<title>` text with commas (`Go,Rust`).

## Run

    npx tsx main.ts
