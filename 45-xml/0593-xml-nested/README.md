# 0593 — Nested element

Uses the `fast-xml-parser` library to parse a fixed catalog document and extract the text of a nested element: the first book's nested `<price>` child (`30`).

## Run

    npx tsx main.ts
