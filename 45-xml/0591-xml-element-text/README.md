# 0591 — Element text

This lesson uses the `fast-xml-parser` library to parse a fixed XML catalog document and read the text content of an element. The first `<book>`'s `<title>` element text is extracted and printed, producing `Go`.

## Run

    npx tsx main.ts
