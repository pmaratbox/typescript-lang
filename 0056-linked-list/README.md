# 0056 — Linked List

Build a singly-linked list holding `1`, `2`, and `3`, then traverse it from head to tail and print `1 -> 2 -> 3`. The `next` field and the traversal cursor are typed `Node | null`, so the compiler requires the `!== null` check before each dereference.

## Run

    npx tsx main.ts
