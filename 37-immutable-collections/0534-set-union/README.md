# 0534 — Set union

Uses Immutable.js's persistent `Set`: calling `a.union(b)` returns a brand-new set containing the elements of both while the original sets stay unchanged. We print the union's elements sorted and space-joined for deterministic output.

## Run

    npx tsx main.ts
