# 0529 — Immutable list

Uses Immutable.js's persistent `List`: calling `push(4)` returns a brand-new list with the appended element while the original list stays unchanged. We print the new list then the original to demonstrate the immutability.

## Run

    npx tsx main.ts
