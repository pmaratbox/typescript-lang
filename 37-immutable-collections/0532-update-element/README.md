# 0532 — Update an element

Uses Immutable.js's persistent `List`: calling `set(0, 99)` returns a brand-new list with index 0 replaced while the original list stays unchanged. We print the new list then the original to demonstrate the immutability.

## Run

    npx tsx main.ts
