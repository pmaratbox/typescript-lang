# 0583 — Select by tag

This lesson uses the `cheerio` library to parse a fixed HTML document and query it with the CSS tag selector `h1`. The single `<h1>` element is selected and its text is printed, producing `Hello`.

## Run

    npx tsx main.ts
