# 0587 — Read an attribute

Uses the [cheerio](https://cheerio.js.org/) HTML-parsing library to load a fixed HTML document and query it with the tag selector `a`, then reads the matched element's `href` attribute with `.attr("href")`.

## Run

    npx tsx main.ts
