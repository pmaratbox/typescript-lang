# 0586 — Select all matching

Uses the **cheerio** HTML-parsing library to query a fixed HTML document. The CSS selector `.item` matches every element with class `item`; we map each matched element to its text and join the results with commas.

## Run

    npx tsx main.ts
