# 0590 — Count matches

Uses the **cheerio** HTML-parsing library to query a fixed HTML document. The CSS selector `.item` matches every element with class `item`; the result set's `.length` gives the count of matching elements.

## Run

    npx tsx main.ts
