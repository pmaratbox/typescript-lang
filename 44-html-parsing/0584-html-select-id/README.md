# 0584 — Select by id

Uses the [cheerio](https://cheerio.js.org/) HTML-parsing library to load a fixed HTML document and query it with the CSS id selector `#status`, printing the matched element's text.

## Run

    npx tsx main.ts
