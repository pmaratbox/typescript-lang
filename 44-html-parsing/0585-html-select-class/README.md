# 0585 — Select by class

Uses the [cheerio](https://cheerio.js.org/) HTML-parsing library to load a fixed HTML document and query it with the CSS class selector `.item`, then prints the text of the first matched element.

## Run

    npx tsx main.ts
