# 0100 — Static Methods

Define a static (class-level) method `square` that needs no instance, call it as `square(6)`, and print `36`. The same `static` class method as JavaScript with typed parameter and return.

## Run

    npx tsx main.ts
