# 0150 — Builder Pattern

Use a fluent builder to construct a Pizza of size M with cheese, and print `Pizza(M, cheese)`. Each builder method returns `this` so calls chain fluently before `build()` produces the immutable Pizza.

## Run

    npx tsx main.ts
