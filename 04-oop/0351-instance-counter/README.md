# 0351 — Instance Counter

Track how many instances of a class have been created; after building three, print `3`. A static class field is incremented in the constructor and read after three creations.

## Run

    npx tsx main.ts
