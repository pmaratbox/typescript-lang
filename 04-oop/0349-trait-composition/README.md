# 0349 — Trait Composition

Compose two capabilities (A printing "a", B printing "b") into one type and invoke both, printing `a b`. Mixin functions layer each trait onto a base class so the composed type gains both contributed methods.

## Run

    npx tsx main.ts
