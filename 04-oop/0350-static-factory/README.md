# 0350 — Static Factory Method

Use a static factory Color.fromHex("#ff0000") to build a color and print its components `255 0 0`. A private constructor forces construction through the static factory that parses the hex pairs into r, g, b.

## Run

    npx tsx main.ts
