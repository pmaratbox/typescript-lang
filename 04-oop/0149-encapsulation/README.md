# 0149 — Encapsulation

Keep an account balance private, deposit 50 onto an initial 100 through a method, and print `150`. TypeScript's `#` private field truly hides the balance at runtime, exposed only via a method and getter.

## Run

    npx tsx main.ts
