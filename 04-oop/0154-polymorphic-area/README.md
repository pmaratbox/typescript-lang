# 0154 — Polymorphic Area

Sum the areas of a Rectangle(2,3)=6 and a Triangle(base=4,height=4)=8 through a common Shape interface, printing `total area: 14`. Both classes implement the `Shape` interface, so a single loop calls `area()` polymorphically.

## Run

    npx tsx main.ts
