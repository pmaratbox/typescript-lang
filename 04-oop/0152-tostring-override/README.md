# 0152 — toString Override

Override the string conversion of a Point(1,2) so it prints `Point(1, 2)`. Defining `toString()` lets template-literal interpolation invoke it automatically.

## Run

    npx tsx main.ts
