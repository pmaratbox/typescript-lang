# 0347 — Virtual Dispatch

Store a Circle, Square, and Triangle in one Shape collection and call describe() on each, printing `circle square triangle`. Each subtype overrides describe() and the base-typed array resolves the call dynamically.

## Run

    npx tsx main.ts
