# 0348 — Interface Default Method

Define an interface with a default greet() returning "hi" and an implementer that overrides it to "hey", printing `hi hey`. TypeScript interfaces carry no bodies, so an abstract base class supplies the default that a subclass overrides.

## Run

    npx tsx main.ts
