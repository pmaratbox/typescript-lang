# 0121 — Parse CSV

Parse the two CSV rows `alice,30` and `bob,25` into name=value pairs and print `alice=30 bob=25`. Destructuring the comma-split array binds each field to a named variable.

## Run

    npx tsx main.ts
