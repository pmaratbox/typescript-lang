# 0119 — Read File Lines

Write three lines to a file, read them back, and print `lines: 3`. Node's `fs` synchronous helpers read the whole file, then `split` and `filter` count the non-empty lines.

## Run

    npx tsx main.ts
