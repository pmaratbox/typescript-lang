# 0120 — Append to a File

Write "a" to a file, append "b", then read both lines back and print `a b`. `writeFileSync` creates the file and `appendFileSync` adds to it without truncating.

## Run

    npx tsx main.ts
