# 0124 — Path Manipulation

Join "/tmp" and "file.txt", then take the basename and extension, printing `/tmp/file.txt file.txt .txt`. The `path.posix` namespace keeps forward-slash separators on every platform.

## Run

    npx tsx main.ts
