# 0125 — Temp File Roundtrip

Write a string to a temporary file, read it back, confirm it matches, delete the file, and print `roundtrip: ok`. `os.tmpdir()` locates the system temp directory for a throwaway file.

## Run

    npx tsx main.ts
