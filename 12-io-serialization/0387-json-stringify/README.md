# 0387 — JSON Stringify

Serialize the object {name:"Ada", age:36} to compact JSON `{"name":"Ada","age":36}`. `JSON.stringify` emits keys in insertion order with no spaces by default.

## Run

    npx tsx main.ts
