# 0051 — Ternary & Conditional Expressions

For each of `4` and `7`, choose `even` or `odd` with a single conditional expression and print `4: even` and `7: odd`. The same `?:` ternary as JavaScript, with both branches checked to a common type. TypeScript narrows the condition within each branch.

## Run

    npx tsx main.ts
