# 0268 — Cellular Automaton Rule 90

Apply one Rule 90 step (new = left XOR right) to the row "00100" with zero boundaries, printing `01010`. A `map` with bounds-checked neighbors and the `^` operator keeps the rule concise.

## Run

    npx tsx main.ts
