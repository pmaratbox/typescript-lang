# 0271 — DFA String Match

Build a DFA for the pattern "ab" and report the first match index in "aab", printing `1`. Advancing a numeric state over the text mirrors a compiled pattern DFA in TypeScript.

## Run

    npx tsx main.ts
