# 0267 — Game of Life Step

Advance one step of a vertical blinker on a 3x3 grid (it becomes horizontal); print the grid using . and #. Mapping over nested arrays expresses Conway's rules functionally in TypeScript.

## Run

    npx tsx main.ts
