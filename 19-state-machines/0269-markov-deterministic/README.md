# 0269 — Deterministic Transition Walk

Walk a deterministic transition map A->B->C->A for three steps from A, printing the visited states `B C A`. A `Record` lookup table drives each step idiomatically.

## Run

    npx tsx main.ts
