# 0322 — Lazy Filter + Take

From a lazy stream of naturals, filter the even ones and take three, printing `2 4 6`. Generators compose lazily so only the needed elements are produced.

## Run

    npx tsx main.ts
