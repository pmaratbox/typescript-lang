# 0331 — Transducer Pipeline

Compose map(+1) with filter(even) and run it over [1,2,3,4], printing `2 4`. Transducers are reducer transformers composed once and applied in a single reduce pass.

## Run

    npx tsx main.ts
