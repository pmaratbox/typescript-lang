# 0332 — Y Combinator

Define factorial via a fixed-point combinator (no named self-recursion) and compute 5!, printing `120`. A Y combinator built from self-application supplies recursion to a non-recursive factorial generator.

## Run

    npx tsx main.ts
