# 0325 — Point-Free Style

Express "sum of squares" point-free (compose map-square with sum) and apply it to [1,2,3], printing `14`. A generic `compose` helper builds the function without naming its argument.

## Run

    npx tsx main.ts
