# 0329 — CPS Factorial

Compute 5! in continuation-passing style, printing `120`. Each recursive step threads a continuation closure, with identity as the base continuation.

## Run

    npx tsx main.ts
