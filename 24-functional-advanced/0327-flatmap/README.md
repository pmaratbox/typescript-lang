# 0327 — FlatMap

FlatMap [1,2,3] with x -> [x, x*10] and print the flattened result `1 10 2 20 3 30`. TypeScript's built-in `Array.prototype.flatMap` maps then concatenates in one call.

## Run

    npx tsx main.ts
