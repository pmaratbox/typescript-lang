# 0218 — Flyweight

Share flyweight instances for repeated characters a, b, a and print the number of distinct objects created `2`. A factory caches one `Glyph` per key, counting only genuine creations.

## Run

    npx tsx main.ts
