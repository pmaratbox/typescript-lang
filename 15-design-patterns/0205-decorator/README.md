# 0205 — Decorator

Decorate a base coffee (cost 2) with milk (+1) and sugar (+1), printing the total cost `4`. Each decorator wraps a `Coffee` and adds to the delegated `cost()`.

## Run

    npx tsx main.ts
