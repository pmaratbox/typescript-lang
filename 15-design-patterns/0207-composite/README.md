# 0207 — Composite

Sum a composite tree of leaf values 1, 2, 3 through a uniform size() interface, printing `6`. Both `Leaf` and `Composite` implement the same `Component` interface.

## Run

    npx tsx main.ts
