# 0201 — Factory Method

Use a factory method to create shapes by name and print their kinds `circle square`. A factory function maps the name strings to typed `Shape` instances.

## Run

    npx tsx main.ts
