# 0212 — Template Method

Run an algorithm skeleton whose middle step is overridden, printing `start work end` on one line. An abstract `run()` fixes the skeleton while subclasses supply the `step()`.

## Run

    npx tsx main.ts
