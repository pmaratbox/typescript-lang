# 0210 — Strategy

Select an "add" then a "mul" strategy and apply each to (3,4), printing `7 12`. Interchangeable function-typed strategies are selected by key and applied.

## Run

    npx tsx main.ts
