# 0209 — Observer

Notify two observers of a new value 5; each prints its id and the value on its own line. The `Subject` keeps an array of `Observer` instances and calls `update` on each.

## Run

    npx tsx main.ts
