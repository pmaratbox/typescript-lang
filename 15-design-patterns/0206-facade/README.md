# 0206 — Facade

Expose a single facade call that starts three subsystems and reports `ready`. The `Facade` hides the three subsystem `init()` calls behind one `start()` method.

## Run

    npx tsx main.ts
