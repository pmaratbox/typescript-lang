# 0219 — Bridge

Bridge a Circle abstraction to a Red color implementation, printing `red circle`. The `Shape` abstraction holds a `Color` implementor and composes their results.

## Run

    npx tsx main.ts
