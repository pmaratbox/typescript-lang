# 0217 — Memento

Save state 1, change it to 2, then restore from the memento, printing current then restored `2 1`. A `Memento` captures the originator's state for later `restore()`.

## Run

    npx tsx main.ts
