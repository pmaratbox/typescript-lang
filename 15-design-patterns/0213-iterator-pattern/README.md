# 0213 — Iterator (Custom)

Implement a custom iterator over a range 1..3 and consume it, printing `1 2 3`. A `RangeIterator` exposes `hasNext()`/`next()` driving a consuming loop.

## Run

    npx tsx main.ts
