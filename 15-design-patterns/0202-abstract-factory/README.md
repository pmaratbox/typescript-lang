# 0202 — Abstract Factory

Use a "dark" theme abstract factory to build a button and a checkbox, printing `dark-button dark-checkbox`. A `ThemeFactory` interface produces themed `Button` and `Checkbox` objects.

## Run

    npx tsx main.ts
