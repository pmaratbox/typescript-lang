# 0220 — Null Object

Compare a no-op null logger with a real logger; only the real one records, so print the logged count `1`. The `NullLogger` satisfies the interface while doing nothing.

## Run

    npx tsx main.ts
