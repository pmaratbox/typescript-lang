# 0203 — Prototype (Clone)

Clone a prototype object holding 1, mutate the clone to 2, and show the original is unchanged, printing `1 2`. A `clone()` method returns a fresh independent copy.

## Run

    npx tsx main.ts
