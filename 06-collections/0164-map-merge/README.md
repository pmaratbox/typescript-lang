# 0164 — Merge Maps

Merge {a:1,b:2} and {b:3,c:4} with the right map winning on conflicts, printing `a:1 b:3 c:4`. Object spread `{...left, ...right}` makes the right side right-biased.

## Run

    npx tsx main.ts
