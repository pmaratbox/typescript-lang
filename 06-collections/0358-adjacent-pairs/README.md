# 0358 — Adjacent Pairs

Produce the consecutive pairs of [1,2,3,4], printing `1,2 2,3 3,4`. Slide a window of size two over the typed number array, formatting each as `a,b` and joining with spaces.

## Run

    npx tsx main.ts
