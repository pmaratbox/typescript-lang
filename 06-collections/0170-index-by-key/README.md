# 0170 — Index By Key

Index people [(1,alice),(2,bob)] by id into a map, look up id 2, and print `id 2: bob`. A `Map<number, string>` built from `id`/`name` pairs gives keyed lookup.

## Run

    npx tsx main.ts
