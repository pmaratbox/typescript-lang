# 0009 — Maps

Build a map `{"one": 1, "two": 2, "three": 3}`, look up `"two"`, and print its
value and the map's size. `Map<string, number>` is typed; `.get(key)` returns
`number | undefined`, so the type system reminds you a lookup can miss. `.size`
counts entries.

## Run

    npx tsx main.ts
