# 0360 — Interleave Lists

Interleave [1,3,5] and [2,4,6] element by element, printing `1 2 3 4 5 6`. Walk a shared index over both typed arrays, alternately pushing one element from each.

## Run

    npx tsx main.ts
