# 0167 — Top-K Frequent

Find the 2 most frequent elements of [a,b,a,c,b,a], printing `a b`. A `Map` tallies counts, then sorting entries by count descending takes the top two.

## Run

    npx tsx main.ts
