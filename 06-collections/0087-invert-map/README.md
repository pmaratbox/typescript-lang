# 0087 — Invert a Map

Invert the map `{a: 1, b: 2, c: 3}` (swap keys and values) and print the result sorted by the new key: `1:a 2:b 3:c`. The same swap as JavaScript, typed `Record<number, string>` for the inverted map.

## Run

    npx tsx main.ts
