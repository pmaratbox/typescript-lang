# 0084 — Partition

Partition `1, 2, 3, 4, 5, 6` into evens and odds, printing `evens: 2 4 6` and `odds: 1 3 5`. The same two-`filter` split as JavaScript over a `number[]`.

## Run

    npx tsx main.ts
