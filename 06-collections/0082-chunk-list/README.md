# 0082 — Chunk a List

Split the list `1, 2, 3, 4, 5, 6, 7` into chunks of `3` and print each chunk on its own line: `1 2 3`, `4 5 6`, `7`. The same `slice`-based chunking as JavaScript over a `number[]`.

## Run

    npx tsx main.ts
