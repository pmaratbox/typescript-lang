# 0094 — Linear Search

Linearly scan the array `4, 2, 7, 1, 9` for `7` and print the index where it is found: `found 7 at index 2`. The same left-to-right scan as JavaScript over a `number[]`.

## Run

    npx tsx main.ts
