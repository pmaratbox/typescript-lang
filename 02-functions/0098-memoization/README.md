# 0098 — Memoization

Compute `fibonacci(10)` recursively with memoization (caching each result so it is computed once) and print it: `55`. The same `Map` cache as JavaScript, typed `Map<number, number>` with an `undefined` check for a miss.

## Run

    npx tsx main.ts
