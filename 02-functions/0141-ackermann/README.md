# 0141 — Ackermann Function

Compute the Ackermann function A(2,3) recursively and print `9`. The classic three-branch definition maps directly onto typed recursive calls.

## Run

    npx tsx main.ts
