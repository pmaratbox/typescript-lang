# 0142 — Countdown by Recursion

Recursively count down from 5 to 1, printing `5 4 3 2 1` on one line. An accumulator array collects each value before being joined into a single space-separated line.

## Run

    npx tsx main.ts
