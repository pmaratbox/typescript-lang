# 0096 — Mutual Recursion

Using two mutually recursive functions `isEven` and `isOdd` (each calling the other), report whether `4` and `3` are even, printing `even` and `odd`. The same hoisted mutual recursion as JavaScript with `boolean` return types.

## Run

    npx tsx main.ts
