# 0341 — Recursive List Reverse

Reverse [1,2,3] using recursion (no built-in reverse), printing `3 2 1`. Spread `[...reverse(tail), head]` expresses `reverse(xs)++[x]` without mutation.

## Run

    npx tsx main.ts
