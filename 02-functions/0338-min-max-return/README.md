# 0338 — Return Min and Max

Return both the minimum and maximum of [4,1,7] from one function and print `1 7`. TypeScript returns the pair as a typed tuple `[number, number]`.

## Run

    npx tsx main.ts
