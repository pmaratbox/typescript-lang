# 0340 — Apply Twice

Write applyTwice(f,x)=f(f(x)) and apply it with inc to 3, printing `5`. A generic higher-order function `<T>` types both the function argument and value.

## Run

    npx tsx main.ts
