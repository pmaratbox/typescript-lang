# 0127 — Day of Week (Zeller)

Use Zeller’s congruence to compute the weekday of 2000-01-01 and print `Saturday`. The integer arithmetic uses `Math.floor` to emulate truncating division on `number`.

## Run

    npx tsx main.ts
