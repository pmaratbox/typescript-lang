# 0126 — Leap Year Check

Determine whether 2000, 1900, and 2024 are leap years, printing `yes no yes`. The rule maps cleanly onto a typed `(year: number) => boolean` predicate applied with `Array.map`.

## Run

    npx tsx main.ts
