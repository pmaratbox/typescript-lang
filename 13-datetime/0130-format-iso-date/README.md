# 0130 — Format ISO Date

Format the date y=2026, m=6, d=4 as a zero-padded ISO string `2026-06-04`. Each field is widened to two digits with `String#padStart` inside a template literal.

## Run

    npx tsx main.ts
