# 0004 — Conditionals

Compare `n` against 10 and print whether it's less, equal, or greater. Same
`if`/`else if`/`else` shape as JavaScript; `===` is the preferred equality
check. Edit `n` to `10` or `15` to exercise the other branches.

## Run

    npx tsx main.ts
