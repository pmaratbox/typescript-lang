# 0080 — Deduplicate

Remove duplicates from `1, 2, 2, 3, 1`, keeping the first occurrence of each in order, and print `1 2 3`. The same `Set`-based dedup as JavaScript; the element type is inferred as `number`.

## Run

    npx tsx main.ts
