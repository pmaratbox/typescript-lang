# 0309 — Validate BST

Check whether two trees are valid BSTs: a correct one (yes) and one with an out-of-place node (no), printing `yes no`. Recurse with `(low, high)` bounds tightened at each node using `±Infinity` as the initial range.

## Run

    npx tsx main.ts
