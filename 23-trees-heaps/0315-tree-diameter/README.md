# 0315 — Tree Diameter

Compute the diameter (longest path in edges) of a tree where root has children A and B, and A has children C and D, printing `3`. Track a closure-captured maximum of `leftHeight + rightHeight` while computing heights.

## Run

    npx tsx main.ts
