# 0312 — Tree Height

Compute the height (in nodes) of the BST built from 5,3,8,1,4, printing `3`. Recurse with `1 + Math.max(height(left), height(right))`, treating null as height 0.

## Run

    npx tsx main.ts
