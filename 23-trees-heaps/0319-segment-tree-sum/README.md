# 0319 — Segment Tree Range Sum

Build a segment tree over [1,2,3,4,5] and query the sum of indices 1..3 (inclusive), printing `9`. Use a flat `2n`-sized array with iterative bottom-up build and range query.

## Run

    npx tsx main.ts
