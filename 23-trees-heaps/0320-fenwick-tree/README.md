# 0320 — Fenwick Tree Prefix Sum

Build a Fenwick (BIT) tree over [1,2,3,4,5] and query the prefix sum of the first 4 elements, printing `10`. Walk indices via the `i & -i` low-bit trick for both update and prefix-sum queries.

## Run

    npx tsx main.ts
