# 0316 — Max-Heap Pop All

Build a max-heap from [3,1,4,1,5], pop the top three values, printing `5 4 3`. Store the heap in a plain `number[]`, sifting up on push and down on pop.

## Run

    npx tsx main.ts
