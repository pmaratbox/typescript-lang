# 0314 — Iterative Preorder

Print the iterative (stack-based) preorder traversal of the BST built from 5,3,8,1,4 `5 3 1 4 8`. Pop-and-visit a node, then push its right child before its left so the left is processed first.

## Run

    npx tsx main.ts
