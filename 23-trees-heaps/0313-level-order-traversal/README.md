# 0313 — Level-Order Traversal

Print the breadth-first (level-order) traversal of the BST built from 5,3,8,1,4 `5 3 8 1 4`. Use an array as a FIFO queue, dequeuing with `shift()` and enqueuing each node's children.

## Run

    npx tsx main.ts
