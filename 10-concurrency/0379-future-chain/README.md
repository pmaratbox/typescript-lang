# 0379 — Future Chain

Chain asynchronous steps 5 -> (*2) -> (+1) and print the final value `11`. Promises are chained with `.then`, where each stage is a future transforming the previous result.

## Run

    npx tsx main.ts
