# 0603 — Inline code

Uses the `markdown-it` library (constructed with the `'commonmark'` preset) to render a backtick-wrapped inline code span `` `code` `` to HTML, then strips the trailing newline before printing.

## Run

    npx tsx main.ts
