# 0600 — Bold

Uses the `markdown-it` library (constructed with the `commonmark` preset) to render the Markdown `**bold**` to HTML. The trailing newline that the renderer appends is stripped before printing, producing `<p><strong>bold</strong></p>`.

## Run

    npx tsx main.ts
