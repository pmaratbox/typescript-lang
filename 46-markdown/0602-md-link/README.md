# 0602 — Link

Uses the `markdown-it` library (constructed with the `'commonmark'` preset) to render a Markdown inline link `[text](http://x.com)` to HTML, then strips the trailing newline before printing.

## Run

    npx tsx main.ts
