# 0605 — Blockquote

Uses the `markdown-it` library (constructed with the `commonmark` preset) to render the Markdown blockquote `> quote` to HTML. The trailing newline that the renderer appends is stripped before printing, producing the `<blockquote>` element wrapping a paragraph.

## Run

    npx tsx main.ts
