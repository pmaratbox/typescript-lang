# 0177 — Min Heap (Manual)

Implement a binary min-heap by hand, push 3,1,2, pop all, printing `1 2 3`. An array-backed heap sifts values up on push and down on pop so repeated pops yield ascending order.

## Run

    npx tsx main.ts
