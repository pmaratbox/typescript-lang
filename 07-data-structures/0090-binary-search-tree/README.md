# 0090 — Binary Search Tree

Insert `5, 3, 8, 1, 4` into a binary search tree and print an in-order traversal (which yields the values in sorted order): `1 3 4 5 8`. The same recursive insert and in-order traversal as JavaScript, with `Node | null` types for the links.

## Run

    npx tsx main.ts
