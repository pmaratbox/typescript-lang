# 0060 — Priority Queue

Push `3`, `1`, and `2` into a min-priority-queue, then pop them all and print them in priority (ascending) order: `1 2 3`. The same array-backed priority queue as JavaScript, typed; insertion keeps the array sorted so the front is the minimum.

## Run

    npx tsx main.ts
