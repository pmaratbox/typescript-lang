# 0365 — Multiset Counts

Add 1,1,2 to a multiset; print the count of 1 (2), remove one 1, then print the count of 1 (1), as `2 1`. A `Map<T, number>` with `??`-default increments models element multiplicities idiomatically.

## Run

    npx tsx main.ts
