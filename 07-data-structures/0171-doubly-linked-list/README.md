# 0171 — Doubly Linked List

Build a doubly linked list 1<->2<->3, traverse forward then backward, printing `1 2 3` then `3 2 1` on two lines. Each node's `prev`/`next` fields are typed `LinkedListNode | null`, so the compiler enforces a null check on every walk.

## Run

    npx tsx main.ts
