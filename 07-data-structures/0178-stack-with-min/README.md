# 0178 — Stack With Min

Build a stack that tracks its minimum in O(1); after pushing 3,1,2 print `min: 1`. An auxiliary min-stack mirrors the main stack, recording the running minimum at each level.

## Run

    npx tsx main.ts
