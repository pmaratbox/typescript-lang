# 0179 — Union-Find

Union (0,1) and (2,3), then query connectivity of (0,1)=yes and (0,2)=no, printing `yes no`. A disjoint-set tracks each element's parent, with `find` collapsing paths and `union` linking roots.

## Run

    npx tsx main.ts
