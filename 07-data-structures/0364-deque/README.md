# 0364 — Double-Ended Queue

Using a deque, push 1 and 2 to the back and 0 to the front, then print front-to-back `0 1 2`. A generic class wrapping an array's `unshift`/`push` gives idiomatic typed front/back access.

## Run

    npx tsx main.ts
