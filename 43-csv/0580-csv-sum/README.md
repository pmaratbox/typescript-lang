# 0580 — Sum a numeric column

Uses the **papaparse** library to parse the `name,age,city` CSV text into rows, locates the `age` column by its header name, parses each value to an integer, and prints the sum (30+25+35).

## Run

    npx tsx main.ts
