# 0579 — Extract a column

Uses the **papaparse** library to parse the `name,age,city` CSV text into rows, locates the `age` column by its header name, and joins each data row's age value with commas.

## Run

    npx tsx main.ts
