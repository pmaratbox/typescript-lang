# 0582 — Filter rows

Uses the **papaparse** library to parse the `name,age,city` CSV text into rows, keeps the data rows whose `age` value is greater than 28, and joins the surviving names with commas.

## Run

    npx tsx main.ts
