# 0640 — Read a section

Uses the [node-config](https://www.npmjs.com/package/config) library to load `config/default.json` and read two keys from the `server` section, `server.host` and `server.port`, printing them as `host:port`.

## Run

    npx tsx main.ts
