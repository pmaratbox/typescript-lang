# 0636 — Boolean value

Load the configuration from `config/default.json` with the [node-config](https://github.com/node-config/node-config) library and read the boolean key `debug`. The resolved value is printed with `console.log`, which renders a boolean in lowercase (`true`).

## Run

    npx tsx main.ts
