# 0639 — Integer value

Uses the [node-config](https://www.npmjs.com/package/config) library to load `config/default.json` and read the integer key `retries`, printing its value.

## Run

    npx tsx main.ts
