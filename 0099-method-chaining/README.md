# 0099 — Method Chaining

Build a calculator that supports method chaining — start at `5`, then `.add(3).multiply(2)` — and print the result: `16`. Methods return `this` (the polymorphic `this` type) so the chain type-checks; `result` returns a `number`.

## Run

    npx tsx main.ts
