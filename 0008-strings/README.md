# 0008 — Strings

Given `name = "world"`, print a greeting, the name in uppercase, and its
length. Same string API as JavaScript; the only addition is the `: string`
type annotation, which is erased at compile time.

## Run

    npx tsx main.ts
