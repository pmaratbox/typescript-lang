# 0053 — Binary Search

Binary-search the sorted array `1, 3, 5, 7, 9` for `7` and print the index where it is found: `found 7 at index 3`. Identical to the JavaScript algorithm with explicit `number` types; the midpoint uses `Math.floor` because `/` does not truncate.

## Run

    npx tsx main.ts
