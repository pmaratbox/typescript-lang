# 0295 — Equal Partition

Decide whether [1,5,11,5] can split into two equal-sum subsets, printing `yes`. Reduce to a `boolean[]` subset-sum on half the total after a parity check.

## Run

    npx tsx main.ts
