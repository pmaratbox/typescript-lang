# 0294 — Subset Sum

Decide whether some subset of [3,34,4,12,5,2] sums to 9, printing `yes`. A descending sweep over a `boolean[]` reachability table gives the classic 0/1 DP.

## Run

    npx tsx main.ts
