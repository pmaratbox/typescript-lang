# 0290 — Unique Grid Paths

Count lattice paths from the top-left to bottom-right of a 3x3 grid moving right/down, printing `6`. A single rolling `number[]` row accumulates the path counts in place.

## Run

    npx tsx main.ts
