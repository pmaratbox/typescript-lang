# 0296 — Matrix Chain Order

Find the minimum scalar multiplications to multiply matrices with dimensions [10,20,30,40], printing `18000`. An interval DP over a `number[][]` table tries every split point `k`.

## Run

    npx tsx main.ts
