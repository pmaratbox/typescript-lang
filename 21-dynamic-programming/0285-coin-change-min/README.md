# 0285 — Coin Change (Min Coins)

Find the fewest coins from {1,2,5} summing to 11, printing `3`. A bottom-up `number[]` table seeded with `Infinity` keeps the idiom typed and minimal.

## Run

    npx tsx main.ts
