# 0288 — Longest Increasing Subsequence

Compute the LIS length of [10,9,2,5,3,7,101,18], printing `4`. A quadratic `number[]` DP plus `Math.max(...dp)` keeps it idiomatic and typed.

## Run

    npx tsx main.ts
