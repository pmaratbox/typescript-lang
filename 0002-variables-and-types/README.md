# 0002 — Variables & basic types

Annotate `const` bindings with the three TypeScript primitive types:
`number`, `string`, `boolean`. TypeScript inherits JavaScript's single
`number` type — there is no separate int/float — so the `int`/`float`
labels in the output are just descriptive strings.

## Run

    npx tsx main.ts
