# 0681 — Map

This lesson uses the [Ramda](https://ramdajs.com/) functional library and its `R.map` transform to apply a pure function over a list. We map `x -> x*2` across `[1, 2, 3]` and comma-join the results.

## Run

    npx tsx main.ts
