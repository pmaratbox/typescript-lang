# 0684 — Chunk

This lesson uses the [Ramda](https://ramdajs.com/) functional library and its `R.splitEvery` transform to split a list into fixed-size chunks. We chunk `[1, 2, 3, 4, 5, 6]` into size-2 pieces, join each chunk with `,` and the chunks with `|`.

## Run

    npx tsx main.ts
