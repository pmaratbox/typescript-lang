# 0687 — Zip

This lesson uses the [Ramda](https://ramdajs.com/) functional library and its `R.zipWith` transform to zip two lists. We zip `[1, 2, 3]` with `["a", "b", "c"]`, formatting each pair as `<n><s>`, and comma-join the result.

## Run

    npx tsx main.ts
