# 0682 — Filter

This lesson uses the [Ramda](https://ramdajs.com/) functional library and its `R.filter` transform to keep only the elements of a list that satisfy a predicate. We filter the even numbers out of `[1, 2, 3, 4, 5, 6]` and comma-join the results.

## Run

    npx tsx main.ts
