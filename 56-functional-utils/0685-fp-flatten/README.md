# 0685 — Flatten

This lesson uses the [Ramda](https://ramdajs.com/) functional library and its `R.flatten` transform to flatten a list of lists. We flatten `[[1, 2], [3, 4], [5, 6]]` one level and comma-join the result.

## Run

    npx tsx main.ts
