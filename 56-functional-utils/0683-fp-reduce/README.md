# 0683 — Reduce

Reduce/fold `[1,2,3,4,5]` with addition starting from `0` to get `15`. Ramda's `R.reduce` folds a list left-to-right with an accumulator function and an initial value, returning the single accumulated result.

## Run

    npx tsx main.ts
