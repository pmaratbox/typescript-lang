# 0076 — Count Vowels

Count the vowels (`a`, `e`, `i`, `o`, `u`) in the word `hello` and print the total: `2`. The same membership loop as JavaScript; `count` is inferred as a `number`.

## Run

    npx tsx main.ts
