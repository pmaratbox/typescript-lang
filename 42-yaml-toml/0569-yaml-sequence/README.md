# 0569 — YAML sequence

This lesson uses the `js-yaml` library to parse a YAML document containing a sequence (list) under the `fruits` key. `yaml.load` turns the block-style list into a JavaScript array of strings, which is then joined with commas to produce `apple,banana,cherry`.

## Run

    npx tsx main.ts
