# 0116 — Function Pipeline

Compose inc, double, and negate into a left-to-right pipeline and apply it to 3: ((3+1)*2) negated = `-8`. A variadic `pipe` folds the functions over the input with `reduce`.

## Run

    npx tsx main.ts
