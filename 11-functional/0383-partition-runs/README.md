# 0383 — Partition Into Runs

Partition [1,1,2,3,3,3] into runs of equal adjacent elements, printing `1 1|2|3 3 3`. In TypeScript a reduce-style fold over the array groups consecutive equals into nested arrays.

## Run

    npx tsx main.ts
