# 0110 — Currying

Curry a two-argument add into a chain of one-argument functions and call it as `add(2)(3)`, printing `5`. An arrow function returning an arrow function captures the first argument in its closure.

## Run

    npx tsx main.ts
