# 0111 — Partial Application

Partially apply a two-argument add by fixing the first argument to 10, then call the result with 3 to print `13`. A closure binds the first argument, leaving a one-argument function.

## Run

    npx tsx main.ts
