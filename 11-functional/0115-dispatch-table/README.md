# 0115 — Dispatch Table

Store functions in a map keyed by name, then apply "add" and "mul" to (3,4), printing `7 12`. A `Record<string, BinOp>` maps names to functions that are looked up and called.

## Run

    npx tsx main.ts
