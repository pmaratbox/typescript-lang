# 0284 — Trie Autocomplete

Insert "car","card","dog" into a trie and autocomplete the prefix "car", printing `car card`. A typed `Map`-backed trie with a recursive DFS collects matches idiomatically in TypeScript.

## Run

    npx tsx main.ts
