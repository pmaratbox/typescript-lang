# 0273 — KMP Search

Use Knuth-Morris-Pratt to find all start indices of "ab" in "ababab", printing `0 2 4`. A typed prefix-function array drives the scan idiomatically in TypeScript.

## Run

    npx tsx main.ts
