# 0282 — Boyer-Moore Search

Use the bad-character rule to find "abc" in "zzabc", printing the index `2`. A typed `Map` of last-occurrence positions implements the heuristic idiomatically in TypeScript.

## Run

    npx tsx main.ts
