# 0278 — Suffix Array

Build the suffix array of "banana" (sorted suffix start indices), printing `5 3 1 0 4 2`. A typed comparator over sliced suffixes sorts the indices idiomatically in TypeScript.

## Run

    npx tsx main.ts
