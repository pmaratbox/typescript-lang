# 0280 — Group Anagrams

Group ["eat","tea","tan","ate","nat"] into anagram classes and print the number of groups `2`. A typed `Map` keyed by sorted letters buckets the words idiomatically in TypeScript.

## Run

    npx tsx main.ts
