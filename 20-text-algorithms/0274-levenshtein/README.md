# 0274 — Levenshtein Distance

Compute the edit distance between "kitten" and "sitting", printing `3`. A single rolling DP row keeps the typed implementation compact in TypeScript.

## Run

    npx tsx main.ts
