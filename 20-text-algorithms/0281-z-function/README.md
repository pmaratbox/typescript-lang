# 0281 — Z-Function

Compute the Z-array of "aaaa" (excluding z[0]) and print `3 2 1`. A typed array with the classic l/r window keeps the linear scan idiomatic in TypeScript.

## Run

    npx tsx main.ts
