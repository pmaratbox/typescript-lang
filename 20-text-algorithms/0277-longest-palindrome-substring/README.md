# 0277 — Longest Palindromic Substring

Find the longest palindromic substring of "babad" (first of maximal length), printing `bab`. A small expand-around-center helper keeps the typed scan idiomatic in TypeScript.

## Run

    npx tsx main.ts
