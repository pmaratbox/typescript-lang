# 0066 — Multiple Assignment & Destructuring

Swap two variables (`a = 1`, `b = 2`) with a single multiple-assignment, then unpack the pair `(3, 4)` into two variables — printing `2 1` then `3 4`. The same array destructuring as JavaScript; the binding types are inferred as `number`.

## Run

    npx tsx main.ts
