# 0231 — Template Substitution

Substitute the variable in the template "hi {name}" with name="Ada", printing `hi Ada`. A regex replace callback pulls each {key} from the vars map.

## Run

    npx tsx main.ts
