# 0230 — Parse JSON Array

Parse the JSON array text "[1,2,3]" and print the sum of its elements `6`. Stripping the brackets and reducing the split tokens keeps it hand-rolled.

## Run

    npx tsx main.ts
