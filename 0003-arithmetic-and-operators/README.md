# 0003 — Arithmetic & operators

Compute and print the five elementary arithmetic operations on two integers
(`a = 17`, `b = 5`). TypeScript inherits JavaScript's single `number` type,
so `17 / 5 → 3.4`. Use `Math.trunc(a / b)` for truncated integer quotient.

## Run

    npx tsx main.ts
