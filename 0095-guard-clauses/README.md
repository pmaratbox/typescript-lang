# 0095 — Guard Clauses

Classify each of `-1`, `0`, and `5` using guard clauses (an early return for each special case) and print `negative`, `zero`, and `positive`. The same early-return guards as JavaScript with a `number` parameter and `string` return type.

## Run

    npx tsx main.ts
