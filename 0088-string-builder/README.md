# 0088 — String Builder

Use a string builder to assemble `1`, `2`, and `3` joined by `-`, printing `1-2-3`. The same array-as-builder as JavaScript; the array is typed to hold the separator strings and the numbers.

## Run

    npx tsx main.ts
