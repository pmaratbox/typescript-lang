# 0261 — Rotate Left (8-bit)

Rotate the 8-bit value 1 left by 1 (->2) and 128 left by 1 (->1), printing `2 1`. A typed `rol8` combines the shifted halves and masks back to 8 bits with `& 0xff`.

## Run

    npx tsx main.ts
