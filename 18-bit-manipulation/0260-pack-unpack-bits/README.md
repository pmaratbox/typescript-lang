# 0260 — Pack and Unpack Bits

Pack r=1,g=2,b=3 into one integer (8 bits each) then unpack them, printing `1 2 3`. Shifts compose the fields and `& 0xff` masks each one back out.

## Run

    npx tsx main.ts
