# 0236 — Big Number Addition

Add the large numbers 999999999999 and 1 as digit strings, printing `1000000000000`. Iterate the strings from the right with `charCodeAt` and a carry to stay clear of native overflow.

## Run

    npx tsx main.ts
