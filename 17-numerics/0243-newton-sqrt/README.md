# 0243 — Newton Square Root

Approximate sqrt(2) with Newton iteration and print it to 4 decimals `1.4142`. The loop refines `x` until the step stabilises, then `toFixed(4)` formats it.

## Run

    npx tsx main.ts
