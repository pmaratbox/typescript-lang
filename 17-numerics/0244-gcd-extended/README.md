# 0244 — Extended Euclid

Compute extended gcd(30,12) returning g and Bezout coefficients, printing `6 1 -2` (since 30*1+12*(-2)=6). A recursive helper returns the `[g, x, y]` triple as a typed tuple.

## Run

    npx tsx main.ts
