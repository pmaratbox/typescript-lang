# 0417 — BehaviorSubject

Implement a BehaviorSubject that holds a current value and replays it immediately to each new subscriber. A typed `BehaviorSubject<T>` stores the latest value and `subscribe` pushes it to the new observer before any further `next`.

## Run

    npx tsx main.ts
