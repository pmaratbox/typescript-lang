# 0408 — Take Operator

Implement take(n) over an unbounded source of the natural numbers, emitting the first 3 then completing (and unsubscribing the source). A `closed` flag on the bridging observer lets the infinite producer loop stop the moment the count is reached.

## Run

    npx tsx main.ts
