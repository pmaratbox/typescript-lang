# 0413 — Zip Streams

Implement zip that pairs values by index and combines them; zip [1,2,3] with [10,20,30] using a+b. Per-source arrays act as queues, draining a matched pair with shift() whenever both are non-empty.

## Run

    npx tsx main.ts
